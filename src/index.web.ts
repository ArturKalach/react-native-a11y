/**
 * Web entry (Step 0 skeleton).
 *
 * Web is a best-effort, View-mocked target (not primary). The current skeleton
 * components are already plain `View` passthroughs, so the default entry is
 * web-safe; this file re-exports it. As native-backed components land in later
 * steps, add `.web` View-mock variants and re-route them here.
 */
export * from './index';
